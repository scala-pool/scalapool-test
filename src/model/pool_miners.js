'use strict';
require('module-alias/register');
const category = __filename.replace(process.cwd() + '/tests', '');
const test = require('ava');
const adddress = require('@scalapool/model/pool_miners');

test.skip(category + ' : clearTotalMiners', t => {

});

test.skip(category + ' : onConnect', t => {

});

test.skip(category + ' : onDisconnect', t => {

});

test.skip(category + ' : onSwitch', t => {

});

test.skip(category + ' : getStats', t => {

});
