'use strict';
require('module-alias/register');
const category = __filename.replace(process.cwd() + '/tests', '');
const test = require('ava');
const number = require('@scalapool/utils/number');

test.skip(category + '  : cleanup', t => {

});

test.skip(category + '  : truncate', t => {

});

test.skip(category + '  : isString', t => {

});
