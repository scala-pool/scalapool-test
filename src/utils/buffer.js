'use strict';
require('module-alias/register');
const category = __filename.replace(process.cwd() + '/tests', '');
const test = require('ava');
const buffer = require('@scalapool/utils/buffer');

test.skip(category + '  : testNoncePattern', t => {

});

test.skip(category + '  : instanceId', t => {

});

test.skip(category + '  : bignum', t => {

});
