'use strict';
require('module-alias/register');
const category = __filename.replace(process.cwd() + '/tests', '');

test.skip(category + ' : addJob', t => {
	
});

test.skip(category + ' : getJobById', t => {
	
});

test.skip(category + ' : cached', t => {
	
});

test.skip(category + ' : fetch', t => {
	
});

